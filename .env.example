# Generate a strong SECRET_KEY using: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your_generated_secret_key_here

# Generate an AUTH_TOKEN using: python -c "import secrets; print(secrets.token_hex(16))"
AUTH_TOKEN=your_generated_auth_token_here

# Rate limiting
RATE_LIMIT=10
RATE_LIMIT_WINDOW=60

# File size limits (5MB)
MAX_CONTENT_LENGTH=5242880

# Cache directory
CACHE_DIR=cache

# OCR language codes for EasyOCR. Defaults include English, Persian, and Arabic.
OCR_LANGUAGES=en,fa,ar

# Optional EasyOCR recognition network override. Leave empty for multilingual auto-selection.
OCR_RECOG_NETWORK=
